# neo4j env vars. These are read from the gateway's `.env` (local) or from
# `/opt/rndexpart/.env` on the VPS (production), so set them there — the
# parent compose loads .env automatically.

NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme
